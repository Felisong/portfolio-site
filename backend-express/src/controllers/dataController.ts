import { Response, Request } from "express";
/// import functions from Repository when i make them

// here create the functions to receive and parse data caro.
// ex:
// export const createNewItem = async (req: Request, res: Response) => {
//   const { name } = req.body;
//   try {
//     const newItem = await createItem(name);
//     res.status(201).json(newItem);
//   } catch (error) {
//     res.status(400).json({ message: "Invalid data" });
//   }
// };
